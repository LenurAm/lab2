@model IEnumerable<User>
@{
    ViewData["Title"] = "Index";
}
<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-black">User List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-asp-controller="User" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-square"></i> &nbsp; Create New User
            </a>
        </div>
    </div>
    <br /><br />

<table class="table table-bordered table-striped" style=" width:100%">
    <thead>
        <tr>
           @* <th>
                User Id
            </th>*@
            <th>
                User Name
            </th>
            <th>
                User Email
            </th>
            <th>
                User Address
            </th>
            <th></th>

        </tr>
    </thead>
    <tbody>
        @foreach(var obj in Model)
        {
            <tr>
               @* <td width="20%">
                    @obj.UserId
                </td>*@
                <td width="30%">
                    @obj.UserName
                </td>
                <td width="30%">
                    @obj.Email
                </td>
                <td width="50%">
                    @obj.Address
                </td>
                <td>
                    <div class="w-75 btn-group" role="group">
                            <a asp-controller="User" asp-action="Edit" asp-route-id="@obj.UserId"
                            class="btn btn-primary mx-2"><i class="bi bi-pencil-fill"></i>Edit</a>
                    </div>
                </td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="User" asp-action="Delete" asp-route-id="@obj.UserId"
                        class="btn btn-primary mx-2">Delete</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
</div>
